import React from 'react';

const ContactError = () => (
  <section className="error-msg">
    <div className="error-text">
    <p>Im sorry there seems to have been something that went wrong.</p>
    </div>
  </section>
);

export default ContactError;
